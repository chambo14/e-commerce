import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Vert lime — couleur principale
  static const Color primary = Color(0xFFC3E703);
  static const Color primaryDark = Color(0xFFA8CE00);

  // Teal/menthe — couleur secondaire
  static const Color secondary = Color(0xFF96D1C7);
  static const Color secondaryLight = Color(0xFFBEE8E2);

  // Neutres
  static const Color background = Color(0xFFF7F7F7);
  static const Color surface = Colors.white;
  static const Color cardBackground = Colors.white;

  // Textes
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF888888);
  static const Color textHint = Color(0xFFBBBBBB);

  // UI
  static const Color divider = Color(0xFFEEEEEE);
  static const Color inputFill = Color(0xFFF0F0F0);
  static const Color chipBackground = Color(0xFFF0F0F0);

  // États
  static const Color error = Color(0xFFFF4444);
  static const Color success = Color(0xFF2E7D32);
  static const Color warning = Color(0xFFFFA000);
}
